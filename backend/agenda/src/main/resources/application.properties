spring.application.name=agenda
server.port=${PORT:5005}

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5433/slotone_agenda}
spring.datasource.username=${DB_USERNAME:slotone}
spring.datasource.password=${DB_PASSWORD:slotone_dev}

# Mismo secreto/expiración que usuarios y gateway (validación del Bearer JWT)
bezkoder.app.jwtSecret=${JWT_SECRET:U2kgdmlzIHBhY2VtLCBwYXJhIGJlbGx1bSBNZW1lbnRvIG1vcmk=}
bezkoder.app.jwtExpirationMs=${JWT_EXPIRATION_MS:3600000}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# URL del micro negocios (para leer correo del negocio al notificar nuevas reservas).
slotone.negocios.base-url=${SLOTONE_NEGOCIOS_BASE_URL:http://localhost:5004}

# Email real para HU-S2-09. No guardes claves aquí: usa variables de entorno.
slotone.mail.enabled=${SLOTONE_MAIL_ENABLED:false}
slotone.mail.from=${SLOTONE_MAIL_FROM:no-reply@slotone.local}
slotone.mail.business-to=${SLOTONE_MAIL_BUSINESS_TO:}
# Opcional: cabecera Reply-To (útil con Resend: el From debe ser uno permitido por Resend; las respuestas van acá, ej. hola.slotone@gmail.com).
slotone.mail.reply-to=${SLOTONE_MAIL_REPLY_TO:}
# Si está definido, el envío usa https://api.resend.com (Render plan gratuito bloquea SMTP saliente).
slotone.mail.resend.api-key=${RESEND_API_KEY:}

spring.mail.host=${SMTP_HOST:localhost}
spring.mail.port=${SMTP_PORT:1025}
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:false}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS:false}
spring.mail.properties.mail.smtp.connectiontimeout=${SMTP_CONNECTION_TIMEOUT:5000}
spring.mail.properties.mail.smtp.timeout=${SMTP_TIMEOUT:5000}
spring.mail.properties.mail.smtp.writetimeout=${SMTP_WRITE_TIMEOUT:5000}
